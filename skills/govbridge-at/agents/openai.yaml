interface:
  display_name: "GovBridge AT"
  short_description: "OEGK searches and current-page claim summaries"
  default_prompt: "Use $govbridge-at to search my OEGK reimbursements in Chrome and summarize the requested current-page claim fields."
policy:
  allow_implicit_invocation: true
