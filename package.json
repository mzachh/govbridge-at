{
  "name": "oegk-claim-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "node scripts/build.mjs",
    "audit": "node scripts/audit.mjs",
    "verify": "npm run typecheck && npm test && npm run build && npm run audit"
  },
  "devDependencies": {
    "@types/chrome": "0.1.32",
    "@types/node": "24.3.0",
    "esbuild": "0.25.9",
    "jsdom": "26.1.0",
    "typescript": "5.9.2",
    "vitest": "3.2.7"
  }
}
